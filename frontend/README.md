
  # DoubleTake Notes Mobile UI

  This is a code bundle for DoubleTake Notes Mobile UI. The original project is available at https://www.figma.com/design/UN053laznSkQUAsISIS6TI/DoubleTake-Notes-Mobile-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  